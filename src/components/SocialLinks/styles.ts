import { Styles } from '../../types'

const styles: Styles = {
  contact: {
    cursor: 'pointer',
  },
  text: {
    display: 'inline-block',
    marginLeft: '15px',
    verticalAlign: 'super',
  },
}

export default styles
